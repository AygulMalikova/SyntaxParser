#include "sec.h"
struct node
{
    int data;
    struct node *left;
    struct node *right;
};
typedef int some;
struct node* newNode(int data) {

  struct node* node = (struct node*)malloc();

  node->data = data;
  if (a && (b-2)) {
    r->l.r = &l;
  }
  some i = 10;
  typedef int (*some)(int, char*, some), a, (*r)();
  arr[i]->a /= (12 + (13));
  node->left = NULL;
  node->right = NULL;
  return node;
}


int main()
{
    c->v.r = 1 + 1;
    char c;
    for(c = 'A';c < 'C'; ++c)
       printf("%c ", c);

    return 0;
  return 0;
    return 0;
}
